# 项目治理

## 维护者

当前项目维护者为 [@realSilasYang](https://github.com/realSilasYang)。维护者负责
路线选择、Issue 分类、代码审查、版本发布、安全响应和行为准则执行。

## 决策方式

影响用户可见行为、配置格式、守护正确性、依赖或发布流程的改动，应先通过
Issue 或 Pull Request 公开说明目标、替代方案、兼容性和验证证据。日常修复由
维护者在审查后合并；难以逆转或存在明显分歧的改动，优先保留讨论期并记录最终
理由。没有公开讨论的紧急安全修复可以先私下处理，在修复发布后补充可公开说明。

## 合并标准

改动至少应满足以下条件：

- 问题和预期结果明确，范围没有夹带无关重构。
- 新行为有自动化测试；GUI 或 DPI 行为另附手工验证证据。
- `tests/verify.ps1` 通过，不接触开发者的正式运行配置。
- 新增依赖具有可追溯来源、固定版本、哈希、许可证和 SBOM 关系。
- 计时器、窗口、消息回调、工作进程及原生资源具有可重复执行的清理路径。
- 用户文档、变更日志和兼容性说明与代码同步。

维护者可以要求拆分过大的 Pull Request，或拒绝无法验证、显著扩大维护成本、
破坏数据边界及与项目定位不符的方案。

## 版本与支持

项目采用语义化版本。当前只维护最新发布版本；安全修复是否回移到旧版本由影响
范围和可行性单独决定，不构成长期支持承诺。发布前必须完成全历史泄漏扫描、
自动化验证、GUI 冒烟测试、可复现构建、发行包校验和变更日志整理。

## 维护者变更

新增维护者需要持续、可靠的贡献记录和现有维护者确认。维护者长期无法继续维护
时，应优先把发布权限、未完成事项和安全报告交接给可信贡献者，并在本文件记录
变更。项目尚无满足条件的继任者时，可以进入只读维护状态，但不得把发布凭据交给
未经验证的第三方。

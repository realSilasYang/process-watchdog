# 进程守护小助手 v1.0.0

## 首次发布

### 新增

- 支持守护进程、脚本和快捷方式，并通过基于证据的三态探活、共享调度与自动恢复维持目标运行。
- 提供软件升级保护、深色 GUI、自定义名称与图标、原子配置持久化、撤销与重做、日志及系统集成。
- 提供本地诊断包、Windows 持续集成、资源压力测试、可复现构建、发行校验和 SPDX SBOM。
- 提供快速开始、安装升级、配置、诊断、故障排查、兼容性及贡献文档。

### 架构整理

- 将本机运行配置与仓库示例配置分离。
- 将核心守护、检查、执行、配置、维护、诊断和 GUI 生命周期职责迁移到独立模块。
- 将主窗口图标、按钮交互、运行时适配、守护项命令、状态日志和系统集成迁出根入口。

### 修复

- 修复按钮渲染器在 GDI+ 初始化、退出和重载期间的资源生命周期问题。
- 修复运行日志“导出诊断包”按钮的悬浮状态及点击回调注册顺序。
- 修复后台文件扫描的目录边界判断，以及临时文件被瞬时占用时的处理。

## 恢复说明

`v1.0.0` 的 GitHub Release 页面曾被误删，并于 2026 年 7 月 28 日从未改写的
`v1.0.0` 标签、原锁定工具链和原发布脚本重新构建后恢复。由于旧版编译链没有实现
跨日期字节级可复现，恢复附件的 SHA-256 与 2026 年 7 月 23 日首次构建不同；源码、
版本标签和构建工具版本保持不变。

完整历史参见 [`CHANGELOG.md`](https://github.com/realSilasYang/process-watchdog/blob/main/CHANGELOG.md)。

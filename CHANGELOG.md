# 变更日志

**简体中文** | [English](docs/CHANGELOG.en.md)

本项目遵循[语义化版本](https://semver.org/lang/zh-CN/)，变更类型参考
[Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## [未发布]

## [1.1.0] - 2026-07-27

### 发行版本

- `process-watchdog-1.1.0-windows-x64.exe`：独立可执行版。用户无需安装 AutoHotkey，下载后即可运行。
- `process-watchdog-1.1.0-windows-x64.zip`：完整便携版，包含 EXE、说明文档、许可证、字体及运行所需资源，适合解压后长期使用或手动部署。
- `process-watchdog-1.1.0-source.zip`：完整源码版，包含 AHK 源码、模块、测试、文档和字体资源。源码版需要本机 AutoHotkey v2 x64。

### 重要说明

- `v1.0.0` 尚无自更新能力，升级到 `v1.1.0` 时必须手动下载并完整替换一次发行包；
  此后 EXE 版和源码版均可使用小助手内置更新。
- 程序搜索现在只使用正在运行的 Everything 服务，不再内置全盘扫描回退，也不限制
  应用层结果数量；文件夹批量导入不受影响。
- 通过 Git 获取源码时必须安装 Git LFS 并执行 `git lfs pull`；Release 附带的源码
  ZIP 已包含完整字体资源，无需 Git LFS。
- 软件升级保护仍默认关闭，需按守护项手动启用并结合一次真实升级确认识别结果。

### 新增

- 为 EXE、Git 源码和普通源码加入完整的自更新流程，支持启动时后台检查和手动检查。
  更新前会校验版本、清单、SHA-256 与入口元数据，更新后等待新进程完成配置、窗口和
  核心守护初始化；失败时恢复程序及个人配置。
- 新增简体中文、港繁、台繁、英语、日语、越南语、韩语、西班牙语、法语、葡萄牙语、
  俄语、德语和意大利语本地化。界面默认跟随 Windows，也可在设置中即时切换语言和
  字体，无需重启守护运行时。
- 新增完整浅色主题与“跟随系统”主题选项；深浅色模式共同覆盖主窗口、各级窗口、菜单、
  提示、输入控件、选项卡、滚动条和禁用状态。
- 新增“帮助信息”窗口、反馈入口和捐赠窗口；运行日志与使用说明由同一入口选择打开。
- 主列表新增自动编号列和语义化彩色状态图标；BAT／CMD 项目可在右键菜单查看由小助手
  实际启动并捕获的独立标准输出及错误输出日志。

### 变更

- 重构核心守护调度、目标控制器、任务代际与变更队列，统一暂停、恢复、删除、排序、
  路径变更和撤销／重做的状态提交，减少整表重载、陈旧回调和重复检查。
- 加强 EXE、脚本、快捷方式及安装器间接入口的目标识别；综合进程快照、命令行、工作
  目录、父子关系、权限和文件证据，未知状态不再触发盲目重启。
- 重整设置页为“通用、监控与启动、停止策略、日志、关于”，并统一现代化标签、按钮、
  输入框、下拉框、间距和 DPI 布局；版本、运行环境、更新入口和项目地址集中到关于页。
- 主窗口、上下文菜单及各子窗口统一为无 Emoji 的语义化矢量图标和圆角交互；优化
  ListView 列宽、图标缩放、选中状态、禁用反馈、窗口层级及拖动缩放后的重绘稳定性。
- 按语言重新选择界面内容字体。优先使用已安装字体，其次私有加载随包授权字体或 OFL
  字体，最后回退至对应 Noto 字体；按钮、设置标签和状态栏独立使用系统 UI 粗体。
- `watchdog.ini` 的自动注释现跟随全部十三种界面语言生成；节名、键名和值保持兼容，
  切换语言或重复保存不会累积旧注释。
- 明确同目录 EXE 与源码共用配置、不同目录配置独立；快捷方式和计划任务只保留最后
  选择的日常入口，并可在两种运行形态间安全切换。
- 收敛仓库根目录，将项目治理、贡献、安全、支持、第三方声明、多语言文档、应用资源、
  配置示例和发行输出归入固定目录；README 新增概览图并重写安装、使用、诊断与贡献入口。
- 正式发布改为只允许从 `main` 手动触发。每次发布解析 AutoHotkey 最新稳定版和
  Ahk2Exe 最新发布版，完成全套测试、双次可复现构建、SBOM、校验清单、构建溯源和
  草稿附件核验后再公开，同时保留版本化 EXE 与完整 `dist` 产物。

### 修复

- 修复短命重复启动进程、权限不足、瞬时检查失败或升级过程被误判为目标停止，导致
  单实例程序被错误重启、恢复死循环或升级保护长期等待的问题。
- 修复暂停项在重启后恢复时状态未初始化、手动排序未持久化、删除后撤销触发全部项目
  初始化，以及多选操作与陈旧异步回调覆盖新状态的问题。
- 修复子窗口最小化或关闭连带影响主窗口、底部状态栏及按钮在缩放后重绘不完整、浅色
  禁用色时序错误、选择背景丢失和输入焦点行为不一致的问题。
- 修复多尺寸程序图标缩放模糊、透明背景变黑、非方形图标偏移，以及 SVG 和常见图片
  格式自定义图标的加载与缓存问题。
- 修复自动更新对 Git worktree、重命名入口、清单粒度变化、陈旧检查结果、部分启动
  失败和配置回滚处理不完整的问题，并确保旧入口保留到同卷原子替换前的最后一步。
- 修复最新版 Ahk2Exe 不支持 `/setversion` 导致构建失败，以及更新校验清单解析、
  草稿续传退出码、英文批量导入标点和少量状态文本未本地化的问题。
- 修复 Release 错误附带独立 SBOM 与校验清单的问题；发布页只保留独立 EXE、便携
  ZIP 和源码 ZIP，自动更新改为优先校验 GitHub Release API 提供的资产 SHA-256。

## [1.0.0] - 2026-07-23

### 新增

- 进程、脚本和快捷方式守护。
- 基于证据的三态探活、共享调度与自动恢复。
- 软件升级保护、深色 GUI、自定义名称与图标。
- 原子配置持久化、撤销与重做、日志及系统集成。
- 本地诊断包、Windows 持续集成、资源压力测试、可复现构建、发行校验和 SPDX SBOM。
- 用户快速开始、安装升级、配置、诊断、故障排查、兼容性及贡献文档。
- 完整 Git 历史的 Gitleaks 门禁、CODEOWNERS、项目治理和公开仓库隐私检查。

### 变更

- 将本机运行配置与仓库示例配置分离。
- 将核心守护、检查、执行、配置、维护、诊断和 GUI 生命周期职责迁移到独立模块。
- 将主窗口图标、按钮交互、运行时适配、守护项命令、状态日志和系统集成迁出根入口。
- 将每周 GUI 稳定性测试扩展为 30 分钟完整控件及三级窗口场景。
- 标签发布现在自行执行 GUI 门禁和双次可复现构建，并发布独立 SPDX SBOM、
  完整校验清单及 GitHub 构建溯源证明。
- 记录 GitHub Actions 与实际编译工具的可执行文件哈希，发行包附带
  AutoHotkey 许可证、精确提交的完整对应源码和本次构建工具链快照。
- GitHub Actions 升级到基于 Node.js 24 的当前主版本，并继续固定完整提交 SHA。

### 修复

- 按钮渲染器在 GDI+ 初始化期间保持 DLL 模块引用，退出和重载时能够可靠关闭并重新初始化。
- 运行日志的“导出诊断包”按钮现在按正确顺序注册悬浮状态和点击回调，真实控件能够可靠响应交互。
- 后台文件扫描使用 `DirExist` 明确区分目录边界，临时文件遇到瞬时占用时执行
  短时有界重试；测试分别报告结果协议、实际文件集合和清理失败。

[未发布]: https://github.com/realSilasYang/process-watchdog/compare/v1.1.0...HEAD
[1.1.0]: https://github.com/realSilasYang/process-watchdog/compare/v1.0.0...v1.1.0
[1.0.0]: https://github.com/realSilasYang/process-watchdog/releases/tag/v1.0.0

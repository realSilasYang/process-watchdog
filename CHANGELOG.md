# 📋 更新日志

**简体中文** | [English](docs/CHANGELOG.en.md)

本项目遵循[语义化版本](https://semver.org/lang/zh-CN/)，变更类型参考
[Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## 🚧 [未发布]

## 🎉 版本 [2.0.7] - 2026-08-01

### 📦 发布物说明

- **`fonts.zip`（可选字体包）**：提供首选字体和回退字体，需先安装到 Windows；它不是程序运行必需。
- **`process-watchdog-2.0.7-source.zip`（完整源码版）**：包含 AHK 源码、模块、测试和文档，不含字体，适合审阅、开发或从源码运行；本机需要 AutoHotkey v2 x64。
- **`process-watchdog-2.0.7-windows-x64.zip`（完整便携版，推荐）**：包含 EXE、说明文档、许可证和运行所需资源，不含字体；无需安装 AutoHotkey，适合完整解压后长期使用。
- **Everything（[官方最新版](https://www.voidtools.com/downloads/)）**：为程序搜索提供索引和后台服务；随包 `Everything64.dll` 只是 IPC 客户端，不能替代 Everything 本体。

---

### 🚀 优化

- **进程识别诊断更可解释**：守护对象因现有进程无法可靠核对而延迟启动时，日志会说明观测来源、原因和原因码，并对重复原因做节流，帮助判断是命令行、进程路径、快照还是探活配置导致的不确定。
- **内容迁移过程更透明**：按内容找回移动目标时，会记录缺失稳定等待、搜索根、扫描方式、扫描失败/超时、候选被拒绝、重复副本和未命中等关键阶段，避免用户只能看到“没有迁移”的黑箱结果。
- **Everything 依赖体验更清楚**：程序搜索会区分随包 `Everything64.dll` 与 Everything 本体，后台实例未响应时先尝试定位并启动 Everything；失败时显示具体错误含义、发现过程或官方下载入口。
- **主界面展示更易读**：状态信息列默认宽度从 180 调整为 200，README 界面概览同步替换为当前主窗口截图。
- **发布工程输出更干净**：可复现构建默认写入受控临时目录并自动清理，CI、演练和正式发布工作流显式指定 `dist` 输出，避免本地发布预检遗留构建产物。

---

### 🐛 修复

- **Everything 失败提示稳定性**：修复 Everything 不可用诊断文本拼接在 AutoHotkey 中可能触发三元表达式解析错误的问题。
- **关于页信息列布局**：版本与运行环境信息列现在按关于页分隔线边界计算，不再越过分隔线或被设置表单的右侧余量影响。
- **打开所在位置的文件管理器选择**：当系统默认文件管理器是 Explorer 时继续选中文件；否则改为交给默认文件管理器打开目录。

## 🎉 版本 [2.0.6] - 2026-07-31

### 📦 发布物说明

- **`fonts.zip`（可选字体包）**：提供首选字体和回退字体，需先安装到 Windows；它不是程序运行必需。
- **`process-watchdog-2.0.6-source.zip`（完整源码版）**：包含 AHK 源码、模块、测试和文档，不含字体，适合审阅、开发或从源码运行；本机需要 AutoHotkey v2 x64。
- **`process-watchdog-2.0.6-windows-x64.zip`（完整便携版，推荐）**：包含 EXE、说明文档、许可证和运行所需资源，不含字体；无需安装 AutoHotkey，适合完整解压后长期使用。
- **Everything（[官方最新版](https://www.voidtools.com/downloads/)）**：为程序搜索提供索引和后台服务；随包 `Everything64.dll` 只是 IPC 客户端，不能替代 Everything 本体。

---

### ⚠️ 重要说明

- **发行附件变更**：从 v2.0.6 起不再提供独立 EXE；升级时必须改用完整便携版或源码版。
- **字体加载方式变更**：程序包不再内含或私有加载字体；需要原有首选字体显示效果时，必须下载可选字体包并安装到 Windows。

---

### 🚀 优化

- **精简程序版本**：发行流程仅构建完整便携版和完整源码版，删除独立 EXE 外层启动器、安装脚本及对应测试，避免同一程序维护三套交付路径。
- **系统字体与可选字体包**：运行时只按原有优先级选择 Windows 已安装字体；首选字体和回退字体改为独立下载，程序包及 SBOM 不再包含字体文件。
- **Everything 获取说明**：所有安装、配置和发行文档统一链接官方最新版，并明确 Everything 本体负责索引和后台服务，随包 DLL 仅负责 IPC 通信。
- **发布链路收敛**：构建、可复现性检查、附件清单、下载后验收和 GitHub Release 工作流统一验证两个程序版本及可选字体包，移除失效的独立 EXE 分支。

---

### 🐛 修复

- **主列表焦点清理**：点击主窗口空白表面或列表空白区域后会清除 ListView 的焦点和焦点项，避免已选守护对象继续保留键盘操作焦点。
- **主窗口标题栏操作**：失焦路由不再拦截非客户区鼠标消息，恢复标题栏最小化、关闭、拖动窗口及边框调整大小等 Windows 原生行为。

## 🎉 版本 [2.0.5] - 2026-07-31

### 📦 发布物说明

- **`process-watchdog-2.0.5-windows-x64.exe`（独立可执行版）**：无需安装 AutoHotkey，下载后即可运行，适合快速体验或只需要单个程序文件的用户。
- **`process-watchdog-2.0.5-windows-x64.zip`（完整便携版，推荐）**：包含 EXE、说明文档、许可证、字体及运行所需资源，适合完整解压后长期使用或手动部署。
- **`process-watchdog-2.0.5-source.zip`（完整源码版）**：包含 AHK 源码、模块、测试、文档和字体资源，适合审阅、开发或从源码运行；本机需要 AutoHotkey v2 x64。

---

### ✨ 新增

- **按内容找回移动目标**：直接添加的程序或脚本会保存文件大小与 SHA-256 内容基线。文件本身或上级目录改名、跨目录或跨磁盘移动后，即使移动发生在小助手关闭期间，也可在确认内容完全一致后更新守护路径；文件名、文件 ID 和目录监听不作为身份依据。
- **原生路径编辑深色主题**：主列表的完整路径原位编辑框现在使用当前输入框背景与文字颜色，右键、双击和 F2 三种入口保持一致。

---

### 🚀 优化

- **高效且保守的迁移扫描**：优先递归检查旧路径最近仍存在的目录，扩大范围后使用 Everything 按扩展名和精确大小缩小候选，再在独立工作进程中计算完整哈希；多个相同副本或未完成扫描不会触发猜测迁移。
- **批量路径编辑**：多选后逐项编辑完整路径时，按 Enter 保存当前条目并自动进入下一项；Esc 仍只取消当前编辑。
- **真正结束目标运行**：右键菜单以“结束运行”取代“重新启动”，先暂停该守护对象再按停止策略关闭目标，避免守护立即重新拉起进程。
- **系统入口与反馈**：创建快捷方式会分别写入并读回验证桌面入口和开始菜单“所有应用”入口，再同步通知 Windows Shell；成功时隐藏创建按钮并完整显示反馈文字，错误弹窗的图标、正文和按钮统一居中布局。
- **首帧深色绘制**：主窗口首次可见前在 DWM 遮蔽期间完成标题栏、列表、表头、状态栏和命令按钮的深色绘制，减少深色模式启动时的白色闪帧。
- **公开发行物终验**：正式发布在核对本地构建与 GitHub 元数据并公开后，会重新下载实际托管的独立 EXE、便携 ZIP 和源码 ZIP，再次核对摘要、解压并复用完整发行包校验；发布专用动态工具链快照与普通 CI 固定快照严格分离，避免审计基准混用。

---

### 🐛 修复

- **守护状态收敛**：修复内容迁移回调参数不匹配导致主监控在进程观测前异常、多个实际运行目标长期停在“初始化”，以及缺失目标在“等待进程状态”和“不存在”之间反复切换的问题。
- **主窗口首次显示**：修复顶部除“添加”外的命令按钮偶发需悬停或点击后才绘制，以及底部状态栏首次打开延迟显示的问题。
- **路径编辑入口一致性**：修复 F2 继续交给 ListView 默认处理并创建第二个亮色编辑框的问题；主题热切换也会刷新当前原位编辑框。
- **重载与快捷方式错误**：重载交接改用显式窗口句柄并逐步容错，避免旧窗口恰好退出时出现“Target window not found”；快捷方式创建失败现在报告实际未完成的入口。

## 🎉 版本 [2.0.4] - 2026-07-30

### 📦 发布物说明

- **`process-watchdog-2.0.4-windows-x64.exe`（独立可执行版）**：无需安装 AutoHotkey，下载后即可运行，适合快速体验或只需要单个程序文件的用户。
- **`process-watchdog-2.0.4-windows-x64.zip`（完整便携版，推荐）**：包含 EXE、说明文档、许可证、字体及运行所需资源，适合完整解压后长期使用或手动部署。
- **`process-watchdog-2.0.4-source.zip`（完整源码版）**：包含 AHK 源码、模块、测试、文档和字体资源，适合审阅、开发或从源码运行；本机需要 AutoHotkey v2 x64。

---

### ✨ 新增

- **主列表键盘控制**：主列表持有焦点时可按空格键暂停或恢复选中的守护对象；支持多选并逐项反转混合状态，长按产生的重复按键不会导致状态来回切换。

---

### 🚀 优化

- **守护对象术语**：界面、日志、配置注释和十三种语言文档统一使用符合功能语义的“守护对象”表达，减少“应用程序”“监控项”或“项目”在不同位置指代不一致的问题。
- **品牌与界面概览**：更新高清透明 Logo 和应用程序 ICO，并同步用于 README、程序窗口、托盘、任务栏和发行包；README 界面概览同时替换为当前主窗口画面。
- **发行说明结构**：历史及未来发行说明不再混入自动化验证清单，版本变化与三种下载方式保持清晰分区，完整验证证据继续保存在项目记录和 Actions 日志中。
- **发布预检稳定性**：新增本机一键发布预检入口，自动准备经上游摘要校验的便携 PowerShell 7、刷新构建工具并串联完整门禁；发行包字体校验直接读取 OpenType 元数据，不再受 PowerShell、.NET 或系统界面语言返回的本地化族名影响。

---

### 🐛 修复

- **日志路径操作对齐**：日志设置页的“浏览”按钮与上方路径输入框左侧对齐，避免操作入口偏离对应字段。

## 🎉 版本 [2.0.3] - 2026-07-29

### 📦 发布物说明

- **`process-watchdog-2.0.3-windows-x64.exe`（独立可执行版）**：无需安装 AutoHotkey，下载后即可运行，适合快速体验或只需要单个程序文件的用户。
- **`process-watchdog-2.0.3-windows-x64.zip`（完整便携版，推荐）**：包含 EXE、说明文档、许可证、字体及运行所需资源，适合完整解压后长期使用或手动部署。
- **`process-watchdog-2.0.3-source.zip`（完整源码版）**：包含 AHK 源码、模块、测试、文档和字体资源，适合审阅、开发或从源码运行；本机需要 AutoHotkey v2 x64。

---

### ✨ 新增

- **目标更名与移动找回**：直接添加的程序或脚本在小助手运行期间更名、父目录更名或在同一卷内移动后，可在文件系统允许按 Windows 文件 ID 回开时找回新路径；该能力不可用时仅以完整的 Windows 重命名事件作为回退，不按相似名称猜测。
- **路径确认与撤销**：检测到候选后冻结该守护对象的重启流程，由本地化确认窗口展示原路径、新路径和识别依据；确认只更新守护路径并保留名称、图标、参数、环境、运行时和升级保护设置，且支持撤销与重做。

---

### 🚀 优化

- **主窗口命令按钮**：添加、暂停／恢复和删除按钮使用固定图标槽；暂停与恢复符号改用统一几何画布绘制，字符图标按实际栅格墨迹居中，状态切换、主题切换和高 DPI 重绘时不再发生图文跳动。
- **目标状态表达**：等待确认新路径拥有独立的状态图标和异常优先级，列表排序、状态栏和守护控制器能够区分目标缺失、等待确认与正常初始化。
- **README 品牌展示与界面概览**：十三种语言首页参照 CodeBookmark 加入统一居中的高清透明 Logo，并继续共用更新后的当前主窗口预览资源。
- **赞赏入口文案**：捐赠按钮使用更简短的悬浮提示，捐赠窗口与十三种语言 README 以直接的方式引导用户选择支持渠道。

---

### 🐛 修复

- **暂停后恢复更名目标**：修复目标在暂停期间更名后，恢复守护可能一直停留在“初始化”的问题；未启用升级保护时，残留目录活动信号不再阻止目标找回。
- **缺失目标识别时序**：目标文件已更名但旧进程仍在运行、进程快照暂不可用或单实例提示刚关闭时，先核对文件身份再判断是否重启，避免把路径变化误判为程序停止并重复启动。
- **路径迁移事务**：手工编辑与自动找回统一执行目标冲突、快捷方式和运行时配置校验；配置写入失败时回滚整个状态，避免列表、运行态和 INI 持久化结果不一致。

## 🎉 版本 [2.0.2] - 2026-07-29

### 📦 发布物说明

- **`process-watchdog-2.0.2-windows-x64.exe`（独立可执行版）**：无需安装 AutoHotkey，下载后即可运行，适合快速体验或只需要单个程序文件的用户。
- **`process-watchdog-2.0.2-windows-x64.zip`（完整便携版，推荐）**：包含 EXE、说明文档、许可证、字体及运行所需资源，适合完整解压后长期使用或手动部署。
- **`process-watchdog-2.0.2-source.zip`（完整源码版）**：包含 AHK 源码、模块、测试、文档和字体资源，适合审阅、开发或从源码运行；本机需要 AutoHotkey v2 x64。

---

### ✨ 新增

- **运行时与搜索发现**：为脚本守护增加通用运行时、解释器、参数和环境配置；Everything 未运行时可静默发现并启动本机实例，未安装时提供官方获取入口。
- **GUI 可访问性语义**：为自绘按钮补充 Windows 按钮角色、本地化默认操作和键盘激活支持，并覆盖生命周期清理。

---

### 🚀 优化

- **窗口层级与任务栏行为**：下级窗口最小化时不再联动最小化上级窗口；恢复时重建所有权、模态状态和任务栏分组，焦点返回直接上级。
- **发布与验证证据**：发行包包含可离线查看的 GUI 验证记录；发布门禁覆盖 13 种语言、主题热切换、窗口资源浸泡和发行包 Markdown 链接。
- **界面概览**：README 的唯一主界面预览更新为当前版本截图，并保持所有发行文档引用同一资源。

---

### 🐛 修复

- **窗口销毁清理**：修复独立 GUI 测试夹具遗漏无障碍服务依赖导致的 `#Warn` 和交互注册残留误报。
- **发行包文档链接**：修复便携包未包含 GUI 验证记录、导致 README 离线相对链接失效的问题。
- **添加窗口初始指针**：窗口激活完成后确认指针仍位于搜索按钮，避免复杂启动时序下指针被系统恢复到旧位置。

## 🎉 版本 [2.0.1] - 2026-07-28

### 📦 发布物说明

- **`process-watchdog-2.0.1-windows-x64.exe`（独立可执行版）**：无需安装 AutoHotkey，下载后即可运行，适合快速体验或只需要单个程序文件的用户。
- **`process-watchdog-2.0.1-windows-x64.zip`（完整便携版，推荐）**：包含 EXE、说明文档、许可证、字体及运行所需资源，适合完整解压后长期使用或手动部署。
- **`process-watchdog-2.0.1-source.zip`（完整源码版）**：包含 AHK 源码、模块、测试、文档和字体资源，适合审阅、开发或从源码运行；本机需要 AutoHotkey v2 x64。

---

### 🚀 优化

- 按实际可见字形而非字体行框计算视觉中心，使 Lucide 图标、按钮文字、主列表状态
  图标和底部状态栏文字在 100% 与 300% DPI 下保持一致的垂直重心。
- 设置页首次构建和切换选项卡时在一次重绘事务内完成控件创建、显隐与标签状态更新，
  减少文字和页面内容先后刷新的闪烁观感。
- README 的全部十三种语言版本统一使用最新主窗口截图作为唯一界面概览，并删除不再
  使用的浅色旧图和双图主题切换结构。
- 更新日志和 GitHub Release 统一采用可扫描的 Emoji 标题与分类结构，逐项说明每种
  发布物的内容、运行要求和适用场景；历史版本说明也按其真实附件重新整理。

---

### 🐛 修复

- 修复字体热切换、状态 SVG 或管理员角标加载异常时主 ListView 图像列表可能解绑，
  导致部分或全部应用图标消失的问题；单个增强图标失败不再影响基础应用图标。
- 修复只指定状态图标纵向偏移时，空横坐标被当成无效整数或图标退到单元格左上角，
  导致状态图标创建失败、偏移或视觉尺寸异常的问题。
- 统一 Windows PowerShell 5.1 与 PowerShell 7 的 UTF-8、JSON、编译路径和 ZIP 字节；
  CI 的两轮发行构建现在强制跨两个宿主比较，不再把“同宿主重复”误称为跨宿主可复现。
- Actions 完整构建产物显式保留隐藏目录，避免解压后的 `.github` 社区文件被上传步骤
  默认过滤；三个 GitHub Release 用户附件不受此前过滤影响。
- 恢复对标 CodeBookmark 的更新日志 Emoji 标题与分类结构；发布门禁现在逐项核对
  三种公开发布物的文件名、版本定位、包含内容、运行要求和适用场景。

---

## 🎉 版本 [2.0.0] - 2026-07-28

### 📦 发布物说明

- **`process-watchdog-2.0.0-windows-x64.exe`（独立可执行版）**：无需安装 AutoHotkey，下载后即可运行，适合快速体验或只需要单个程序文件的用户。
- **`process-watchdog-2.0.0-windows-x64.zip`（完整便携版，推荐）**：包含 EXE、说明文档、许可证、字体及运行所需资源，适合完整解压后长期使用或手动部署。
- **`process-watchdog-2.0.0-source.zip`（完整源码版）**：包含 AHK 源码、模块、测试、文档和字体资源，适合审阅、开发或从源码运行；本机需要 AutoHotkey v2 x64。

---

### ⚠️ 重要说明

- `v1.0.0` 尚无自更新能力，升级到 `v2.0.0` 时必须手动下载并完整替换一次发行包；
  此后 EXE 版和源码版均可使用小助手内置更新。
- 程序搜索现在只使用正在运行的 Everything 服务，不再内置全盘扫描回退，也不限制
  应用层结果数量；文件夹批量导入不受影响。
- 通过 Git 获取源码时必须安装 Git LFS 并执行 `git lfs pull`；Release 附带的源码
  ZIP 已包含完整字体资源，无需 Git LFS。

---

### ✨ 新增

- 为 EXE、Git 源码和普通源码加入完整的自更新流程，支持启动时后台检查和手动检查。
  更新前会校验版本、清单、SHA-256 与入口元数据，更新后等待新进程完成配置、窗口和
  核心守护初始化；失败时恢复程序及个人配置。
- 新增简体中文、港繁、台繁、英语、日语、越南语、韩语、西班牙语、法语、葡萄牙语、
  俄语、德语和意大利语本地化。界面默认跟随 Windows，也可在设置中即时切换语言和
  字体，无需重启守护运行时。
- 新增完整浅色主题与“跟随系统”主题选项；深浅色模式共同覆盖主窗口、各级窗口、菜单、
  提示、输入控件、选项卡、滚动条和禁用状态。
- 新增“帮助信息”窗口、反馈入口和捐赠窗口；运行日志与使用说明由同一入口选择打开。
- 主列表新增自动编号列和语义化彩色状态图标；BAT／CMD 守护对象可在右键菜单查看由小助手
  实际启动并捕获的独立标准输出及错误输出日志。

---

### 🚀 优化

- 重构核心守护调度、目标控制器、任务代际与变更队列，统一暂停、恢复、删除、排序、
  路径变更和撤销／重做的状态提交，减少整表重载、陈旧回调和重复检查。
- 加强 EXE、脚本、快捷方式及安装器间接入口的目标识别；综合进程快照、命令行、工作
  目录、父子关系、权限和文件证据，未知状态不再触发盲目重启。
- 重整设置页为“通用、监控与启动、停止策略、日志、关于”，并统一现代化标签、按钮、
  输入框、下拉框、间距和 DPI 布局；版本、运行环境、更新入口和项目地址集中到关于页。
- 主窗口、上下文菜单及各子窗口统一为无 Emoji 的语义化矢量图标和圆角交互；优化
  ListView 列宽、图标缩放、选中状态、禁用反馈、窗口层级及拖动缩放后的重绘稳定性。
- 按语言重新选择界面内容字体。优先使用已安装字体，其次私有加载随包授权字体或 OFL
  字体，最后回退至对应 Noto 字体；按钮、设置标签和状态栏独立使用系统 UI 粗体。
- `watchdog.ini` 的自动注释现跟随全部十三种界面语言生成；节名、键名和值保持兼容，
  切换语言或重复保存不会累积旧注释。
- 明确同目录 EXE 与源码共用配置、不同目录配置独立；快捷方式和计划任务只保留最后
  选择的日常入口，并可在两种运行形态间安全切换。
- 收敛仓库根目录，将项目治理、贡献、安全、支持、第三方声明、多语言文档、应用资源、
  配置示例和发行输出归入固定目录；README 新增概览图并重写安装、使用、诊断与贡献入口。
- 正式发布改为只允许从 `main` 手动触发。每次发布解析 AutoHotkey 最新稳定版和
  Ahk2Exe 最新发布版，完成全套测试、双次可复现构建、SBOM、校验清单、构建溯源和
  草稿附件核验后再公开，同时保留版本化 EXE 与完整 `dist` 产物。
- 普通 CI 改用仓库内经哈希固定的工具链快照和 Actions 缓存，避免上游临时变化造成
  无关失败；新增只读的手动发布演练，使用与正式发布完全相同的动态工具链与门禁。
- 发布冲突判断、三附件白名单、GitHub 摘要和正文核对收敛为可测试的共享状态机；
  支持恢复同提交草稿或孤立标签，并在公开后再次审计标签、提交、正文和附件哈希。

---

### 🐛 修复

- 修复短命重复启动进程、权限不足、瞬时检查失败或升级过程被误判为目标停止，导致
  单实例程序被错误重启、恢复死循环或升级保护长期等待的问题。
- 修复暂停守护对象在重启后恢复时状态未初始化、手动排序未持久化、删除后撤销触发全部守护对象
  初始化，以及多选操作与陈旧异步回调覆盖新状态的问题。
- 修复子窗口最小化或关闭连带影响主窗口、底部状态栏及按钮在缩放后重绘不完整、浅色
  禁用色时序错误、选择背景丢失和输入焦点行为不一致的问题。
- 修复多尺寸程序图标缩放模糊、透明背景变黑、非方形图标偏移，以及 SVG 和常见图片
  格式自定义图标的加载与缓存问题。
- 修复自动更新对 Git worktree、重命名入口、清单粒度变化、陈旧检查结果、部分启动
  失败和配置回滚处理不完整的问题，并确保旧入口保留到同卷原子替换前的最后一步。
- 修复最新版 Ahk2Exe 不支持 `/setversion` 导致构建失败，以及更新校验清单解析、
  草稿续传退出码、英文批量导入标点和少量状态文本未本地化的问题。
- 修复 Release 错误附带独立 SBOM 与校验清单的问题；发布页只保留独立 EXE、便携
  ZIP 和源码 ZIP，自动更新改为优先校验 GitHub Release API 提供的资产 SHA-256。

## 🎉 版本 [1.0.0] - 2026-07-23

### 📦 发布物说明

- **`process-watchdog-1.0.0-windows-x64.zip`（完整 Windows x64 包）**：包含编译后的程序、说明文档、许可证、状态图标及运行所需依赖；完整解压后即可运行，无需另行安装 AutoHotkey。
- **`process-watchdog-1.0.0-windows-x64.spdx.json`（SPDX 软件物料清单）**：记录应用、内嵌 AutoHotkey 运行时及第三方组件的版本、许可证和来源，供审阅与供应链核验。
- **`SHA256SUMS.txt`（SHA-256 校验清单）**：记录 Windows ZIP 与 SPDX 文件的校验值，用于确认下载内容完整且未在传输过程中损坏。

---

### ✨ 新增

- 进程、脚本和快捷方式守护。
- 基于证据的三态探活、共享调度与自动恢复。
- 软件升级保护、深色 GUI、自定义名称与图标。
- 原子配置持久化、撤销与重做、日志及系统集成。
- 本地诊断包、Windows 持续集成、资源压力测试、可复现构建、发行校验和 SPDX SBOM。
- 用户快速开始、安装升级、配置、诊断、故障排查、兼容性及贡献文档。
- 完整 Git 历史的 Gitleaks 门禁、CODEOWNERS、项目治理和公开仓库隐私检查。

---

### 🚀 优化

- 将本机运行配置与仓库示例配置分离。
- 将核心守护、检查、执行、配置、维护、诊断和 GUI 生命周期职责迁移到独立模块。
- 将主窗口图标、按钮交互、运行时适配、守护对象命令、状态日志和系统集成迁出根入口。
- 将每周 GUI 稳定性测试扩展为 30 分钟完整控件及三级窗口场景。
- 标签发布现在自行执行 GUI 门禁和双次可复现构建，并发布独立 SPDX SBOM、
  完整校验清单及 GitHub 构建溯源证明。
- 记录 GitHub Actions 与实际编译工具的可执行文件哈希，发行包附带
  AutoHotkey 许可证、精确提交的完整对应源码和本次构建工具链快照。
- GitHub Actions 升级到基于 Node.js 24 的当前主版本，并继续固定完整提交 SHA。

---

### 🐛 修复

- 按钮渲染器在 GDI+ 初始化期间保持 DLL 模块引用，退出和重载时能够可靠关闭并重新初始化。
- 运行日志的“导出诊断包”按钮现在按正确顺序注册悬浮状态和点击回调，真实控件能够可靠响应交互。
- 后台文件扫描使用 `DirExist` 明确区分目录边界，临时文件遇到瞬时占用时执行
  短时有界重试；测试分别报告结果协议、实际文件集合和清理失败。

[未发布]: https://github.com/realSilasYang/process-watchdog/compare/v2.0.7...HEAD
[2.0.7]: https://github.com/realSilasYang/process-watchdog/compare/v2.0.6...v2.0.7
[2.0.6]: https://github.com/realSilasYang/process-watchdog/compare/v2.0.5...v2.0.6
[2.0.5]: https://github.com/realSilasYang/process-watchdog/compare/v2.0.4...v2.0.5
[2.0.4]: https://github.com/realSilasYang/process-watchdog/compare/v2.0.3...v2.0.4
[2.0.3]: https://github.com/realSilasYang/process-watchdog/compare/v2.0.2...v2.0.3
[2.0.2]: https://github.com/realSilasYang/process-watchdog/compare/v2.0.1...v2.0.2
[2.0.1]: https://github.com/realSilasYang/process-watchdog/compare/v2.0.0...v2.0.1
[2.0.0]: https://github.com/realSilasYang/process-watchdog/compare/v1.0.0...v2.0.0
[1.0.0]: https://github.com/realSilasYang/process-watchdog/releases/tag/v1.0.0
